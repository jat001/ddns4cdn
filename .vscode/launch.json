{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "main",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/src/main.go",
            "args": ["-c", "${workspaceFolder}/config.toml"],
            "cwd": "${workspaceFolder}"
        }
    ]
}
